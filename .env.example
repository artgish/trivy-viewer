# Storage Configuration
# Supported formats:
#   s3://bucket-name       - AWS S3
#   azure://container-name - Azure Blob Storage
#   gs://bucket-name       - Google Cloud Storage
#   gcs://bucket-name      - Google Cloud Storage (alternate)
#   /path/to/data          - Local filesystem
STORAGE_LOCATION=s3://trivy-findings
STORAGE_PREFIX=

# AWS Configuration (only needed for S3)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key

# Azure Configuration (only needed for Azure Blob)
AZURE_STORAGE_CONNECTION_STRING=

# GCP Configuration (only needed for GCS)
# Uses Application Default Credentials or GOOGLE_APPLICATION_CREDENTIALS

# Server Configuration
PORT=3000
